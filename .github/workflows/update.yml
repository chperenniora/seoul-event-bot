name: 서울 행사 자동 수집

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'

jobs:
  collect:
    runs-on: ubuntu-latest
    permissions:
      contents: write   # ← 로봇한테 강제로 쓰기 권한을 주는 마법의 줄이에요!
    steps:
      - name: 코드 가져오기
        uses: actions/checkout@v4

      - name: 파이썬 준비
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 행사 수집 실행
        env:
          NAVER_ID: ${{ secrets.NAVER_ID }}
          NAVER_SECRET: ${{ secrets.NAVER_SECRET }}
        run: python collect.py

      - name: 결과 저장 (강제 푸시)
        run: |
          git config user.name "bot"
          git config user.email "bot@users.noreply.github.com"
          git add events.json
          git commit -m "행사 자동 업데이트" || echo "변경 없음"
          git push origin HEAD:main
